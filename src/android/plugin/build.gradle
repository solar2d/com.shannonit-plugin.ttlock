

plugins {
    id 'com.android.library'
}

android {
    namespace "plugin.ttlock"
    compileSdkVersion 34
    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"

    }
    

    buildFeatures {
        dataBinding true
        viewBinding true
        buildConfig true
    }

    compileOptions {
        targetCompatibility JavaVersion.VERSION_1_8
        sourceCompatibility JavaVersion.VERSION_1_8
    }
    lintOptions {
        abortOnError false
        checkReleaseBuilds false
        // 防止在发布的时候出现因MissingTranslation导致Build Failed!
        disable 'MissingTranslation'
    }
    //添加本地仓库，并把libs目录作为仓库地址
    repositories {
        flatDir {
            dirs 'libs'
        }
    }
    
}

dependencies {
    //implementation fileTree(include: ['*.aar'], dir: 'libs')
    // implementation(name: 'jnlua-1.0.4', ext: 'jar')
    implementation ':Corona@aar'
    // implementation "androidx.annotation:annotation:1.6.0"

    implementation 'androidx.appcompat:appcompat:1.0.0'
    implementation 'androidx.recyclerview:recyclerview:1.0.0'
    implementation 'com.google.android.material:material:1.0.0'
    implementation 'androidx.constraintlayout:constraintlayout:1.1.3'
//    testImplementation 'junit:junit:4.12'
//    implementation(name: 'ttlock-release-3.0.7', ext: 'aar')
    implementation 'no.nordicsemi.android:dfu:2.5.0'
//    androidTestImplementation 'androidx.test.ext:junit:1.1.1'
//    androidTestImplementation 'androidx.test.espresso:espresso-core:3.1.0'
    implementation 'com.google.code.gson:gson:2.6.2'
    implementation 'com.ttlock:ttlock:3.5.4'
    implementation 'com.squareup.retrofit2:retrofit:2.4.0'
    implementation 'com.squareup.retrofit2:converter-gson:2.4.0'
}

def providerId = 'com.solar2d'
def solar2DVersion = '2020.2630'
def pluginNamespace = android.namespace

tasks.register("copyPluginAAR", Copy) {
    dependsOn assembleRelease
    group = "Solar2Dev"
    description = "Copy plugin AAR to local Solar2D plugin folders"

    // Path to the existing AAR
    def aarFile = file("$buildDir/outputs/aar/plugin-release.aar")

    // Local Solar2D plugin path (APPDATA on Windows, HOME on Unix)
    def rootS2DP = System.env.APPDATA ?: System.env.HOME
    def solar2DPath = file("$rootS2DP/Solar2DPlugins/$providerId/$pluginNamespace/android")

    // Additional folder: plugins/<version>/android
    def additionalPath = file("$projectDir/../../../plugins/$solar2DVersion/android")

    doFirst {
        if (!aarFile.exists()) {
            throw new GradleException("AAR file does not exist: $aarFile")
        }
        println "== Copying plugin AAR: $aarFile =="
    }

    // Copy to Solar2D local repository
    from(aarFile)
    into(solar2DPath)

    // Copy to plugins/<version>/android after the first copy
    doLast {
        copy {
            from(aarFile)
            into(additionalPath)
        }

        println "== Plugin AAR copied to local Solar2D path: $solar2DPath =="
        println "== Plugin AAR also copied to: $additionalPath =="
    }
}

